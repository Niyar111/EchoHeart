1. DEPENDENCIES
Ignore all node_modules folders, both in the root and in subdirectories
**/node_modules
node_modules

2. ENVIRONMENT VARIABLES - CRITICAL
Ignore all .env files to protect secret keys (database passwords, API keys, etc.)
.env
**/.env

3. LOGS
Ignore log files, which can grow very large and contain sensitive information
logs
.log
npm-debug.log
yarn-debug.log*
Ignore all .env files to protect secret keys (database passwords, API keys, etc.)
.env
**/.env

3. LOGS
Ignore log files, which can grow very large and contain sensitive information
logs
.log
npm-debug.log
yarn-debug.log*
yarn-error.log*

4. PRODUCTION / BUILD FOLDERS
Ignore folders created during a build process
/build
/dist

5. SENSITIVE CREDENTIALS - CRITICAL
Ignore the Firebase service account key file
/Backend/serviceAccountKey.json

6. DEVELOPMENT-ONLY SCRIPTS
Ignore any temporary scripts used for testing or development
/Backend/getTestToken.js

7. OPERATING SYSTEM / EDITOR FILES
Ignore files generated by different operating systems and code editors
.DS_Store
.vscode/
.idea/
Thumbs.db